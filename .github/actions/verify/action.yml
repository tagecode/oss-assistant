name: Verify
description: Typecheck, unit/component tests, and production build

runs:
  using: composite
  steps:
    - name: Typecheck
      shell: bash
      run: pnpm typecheck

    - name: Unit & component tests
      shell: bash
      run: pnpm test

    - name: Production build
      shell: bash
      run: pnpm build
